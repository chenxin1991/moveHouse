<view class="tab">
	<view wx:for="{{tabList}}" wx:key="index" data-id="{{item.id}}" bindtap="setTabIndex" class="{{activeIndex===item.id?'active':''}}">{{item.title}}</view>
</view>
 
<view wx:if="{{moduleId===activeIndex}}">
 <!-- 引用组件的页面模板 -->
  <allGoodsList>
    <!-- 这部分内容将被放置在组件 <slot> 的位置上 -->
    <!-- <view>这里是插入到组件slot中的内容</view> -->
  </allGoodsList>
</view>
<!-- <view wx:if="{{activeIndex===1}}">
222222</view>
<view wx:if="{{activeIndex===2}}">
3333333</view>
<view wx:if="{{activeIndex===3}}">
444444
</view>
<view wx:if="{{activeIndex===4}}">
55555</view> -->
 
