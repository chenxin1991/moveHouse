<view class="tab">
<block  wx:for="{{tabList}}" wx:key="index">
<view data-id="{{item.id}}" bindtap="setTabIndex" class="{{activeIndex===item.id?'active':''}}">{{item.title}}</view>
</block>
</view>
 
<view >
 <!-- 引用组件的页面模板 -->
  <allGoodsList bind:myevent="onMyEvent">
    <!-- 这部分内容将被放置在组件 <slot> 的位置上 -->
    <!-- <view>这里是插入到组件slot中的内容</view> -->
  </allGoodsList>
</view>